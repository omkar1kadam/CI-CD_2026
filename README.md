# CI-CD_2026

## Prime numbers

This Maven project computes and prints the first 1,000 prime numbers.

Run the tests:

```bash
mvn test
```

Print the first 1,000 primes:

```bash
mvn compile exec:java -Dexec.mainClass=com.omkar1kadam.primes.Main
```